/** Directory where the polyfilling files are written to */
export const baseOutputDir = "./.wrangler/fs-assets-polyfilling";

/** Directory where the manifest files are written to */
export const baseOutputManifestsDir = `${baseOutputDir}/manifests`;

/** Directory where the fs polyfills are written to */
export const baseOutputPolyfillsDir = `${baseOutputDir}/polyfills`;
